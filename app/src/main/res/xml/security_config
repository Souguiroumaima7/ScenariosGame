<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
<!-- Enforce HTTPS; use only system trust anchors. Add cert pinning if you call APIs. -->
<base-config cleartextTrafficPermitted="false">
<trust-anchors>
<certificates src="system"/>
</trust-anchors>
</base-config>
</network-security-config>
